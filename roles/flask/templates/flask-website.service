Description=uWSGI instance to serve {{ item }}
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/opt/{{ item }}
Environment="PATH=/opt/.venv/flask/bin"
ExecStart=/opt/.venv/flask/bin/uwsgi --ini {{ item }}.ini

[Install]
WantedBy=multi-user.target
