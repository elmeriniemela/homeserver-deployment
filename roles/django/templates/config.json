{
    "POSTGRESQL": {
        "USER": "{{ django_user }}",
        "PASSWORD": "{{ thecodebase_db_password }}"
    },
    "SECRET_KEY": "{{ thecodebase_secret_key }}",
    "ALLOWED_HOSTS": [
        "www.thecodebase.site"
    ],
    "LOG_FILE": "{{ django_home_dir }}/log/django-application.log"
}